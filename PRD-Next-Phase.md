# AI File Sorter — 下一阶段产品需求文档 (PRD)

> 版本：v1.0 | 日期：2026-07-16 | 作者：Alice（产品经理）
> 基于 v2.4.4 (Build 16) 的产品评审

---

## 1. 项目信息

| 项目 | 内容 |
|------|------|
| **语言** | 中文 |
| **目标版本** | v2.5.0 (体验优化) → v3.0.0 (架构升级) |
| **编程语言** | Swift (SwiftUI + AppKit) |
| **最低系统** | macOS 13+ |
| **当前体积** | App 约 4.7MB，安装包约 1.5MB |
| **核心约束** | 纯本地、不上传、无 AI 内嵌、低权限、低内存、无 Docker/Web |

---

## 2. 产品定义

### 2.1 当前状态总结

AI File Sorter 经过 15 个迭代（v2.0.0 → v2.4.4），已具备完整的基础功能闭环：规则匹配 → 预览确认 → 执行整理 → 历史撤销。但在**大文件量场景下的可发现性和操作效率**方面存在明显短板：

- 待分类 200+ 文件时，纯列表无搜索无筛选，靠肉眼滚动
- 整理计划是平铺列表，无分组汇总，执行前无最终确认
- Quick Look 需要手动点击，交互不连贯
- 历史 500 条记录无搜索，查找特定记录困难
- 缺乏统计面板，用户不知道"待处理文件总大小"等信息

### 2.2 下一阶段产品目标

1. **提升大文件量场景的操作效率**：让用户在 200+ 待分类文件中快速定位、筛选和决策
2. **增强整理计划的安全感和可控性**：执行前可再次确认文件状态，避免误操作
3. **改善交互连贯性**：减少不必要的点击和窗口切换
4. **为 v3.0 架构升级做铺垫**：在不增加体积和复杂度的前提下，为代码拆分、性能优化打好基础

---

## 3. 用户故事

| # | 用户故事 | 当前痛点 |
|---|---------|---------|
| US1 | 作为日常使用 Downloads 的用户，我希望能**快速搜索和筛选待分类文件**，这样我不用在 200+ 个文件中逐个滚动查找 | 无搜索框、无筛选器，只能肉眼扫描 |
| US2 | 作为规则使用者，我希望能**在执行前看到整理计划的统计汇总**（多少文件、总大小、按目标目录分组），这样我能快速判断这次整理是否合理 | 整理计划是平铺列表，无摘要信息 |
| US3 | 作为谨慎的用户，我希望能**在最终执行前确认文件仍然存在且未被修改**，这样我不会意外移动被其他程序修改的文件 | `executeOrganizingPlan()` 直接执行，无再次检查 |
| US4 | 作为频繁撤销的用户，我希望能**在整理历史中搜索特定文件**，这样我能快速找到并撤销几天前的一次误整理 | 500 条记录无搜索 |
| US5 | 作为日常使用者，我希望能**通过键盘快速预览文件**（选中即预览），这样我不用每次手动点击 Quick Look 按钮 | Quick Look 需手动触发，不跟随选中项 |

---

## 4. 需求池（按优先级排序）

### 4.1 P0 — 必须做（v2.5.0）

这些需求直接解决当前最痛的效率问题，且实现成本可控。

---

#### P0-1：待分类文件搜索与筛选

**问题描述**：
当 Downloads 累积 200+ 未匹配文件时，`PendingInboxView` 的列表只有文件名和关键词提示，用户必须滚动浏览全部条目。没有搜索框、没有扩展名/大小筛选，效率极低。

**预期方案**：
在待分类列表顶部增加一个搜索/筛选栏：

```
[🔍 搜索文件名或关键词...]  [扩展名▼]  [排序▼]
```

- **搜索**：实时过滤，匹配文件名和关键词字段（前端过滤，不触发磁盘 I/O）
- **扩展名筛选**：下拉多选（pdf、jpg、zip 等），自动从当前待分类列表中提取可选值
- **排序**：默认按文件名、按修改时间、按文件大小
- 筛选条件以轻量标签形式显示在搜索栏下方，可一键清除

**影响范围**：仅前端（`PendingInboxView` + `AppModel.pendingFiles` 过滤逻辑）
**预估代码量**：~80 行
**性能影响**：无（纯内存过滤）
**体积影响**：可忽略

---

#### P0-2：整理计划统计与分组

**问题描述**：
`OrganizingPlanView` 当前是平铺列表，用户只能看到"已选择 N 项"。没有总文件大小、按目标目录分组、重名项数量等统计信息。用户难以快速判断这次整理的影响面。

**预期方案**：
在整理计划列表上方增加统计摘要栏：

```
┌─────────────────────────────────────────────────┐
│ 📊 共 47 个文件，总大小 235.6 MB                  │
│ ├ 办公文档 (→ ~/Documents/下载整理/办公文档): 12 个 │
│ ├ 下载图片 (→ ~/Pictures/下载图片): 18 个           │
│ ├ 压缩文件 (→ ~/Documents/下载整理/压缩文件): 9 个   │
│ └ 屏幕截图 (→ ~/Pictures/截图): 8 个                │
│ ⚠️ 重名文件（将自动编号）: 3 个                     │
└─────────────────────────────────────────────────┘
```

同时在列表中支持按目标目录分组折叠/展开（默认展开）。

**影响范围**：仅前端（`OrganizingPlanView` + 新增统计计算逻辑）
**预估代码量**：~100 行
**性能影响**：统计计算在生成 Plan 时一次性完成，不增加运行时开销
**体积影响**：可忽略

---

#### P0-3：整理计划执行前再确认

**问题描述**：
`executeOrganizingPlan()` 在用户点击"确认整理"后直接调用 Agent 执行，不检查文件是否仍然存在、是否在生成计划后被修改或移动。用户可能在打开整理计划窗口后继续下载/修改文件，实际执行时状态已变化。

**预期方案**：
点击"确认整理"后，弹出一个**轻量确认对话框**：

```
┌──────────────────────────────────────┐
│ 确认执行整理计划                      │
│                                      │
│ 即将移动 47 个文件到 4 个目标目录。     │
│                                      │
│ 🔍 正在检查文件状态...                 │
│   ✓ 44 个文件状态正常                 │
│   ⚠️ 2 个文件已被修改（仍可整理）       │
│   ✗ 1 个文件已不存在（将跳过）          │
│                                      │
│ [取消]  [仍然执行]                    │
└──────────────────────────────────────┘
```

检查逻辑：
1. 遍历所选文件的 `sourcePath`，确认 `FileManager.fileExists`
2. 对比文件大小/修改时间与生成 Plan 时记录的快照（需在 `OrganizingPlanItem` 中增加 `fileSize` 和 `modificationDate` 字段）
3. 显示三种状态：正常 / 已变化（可继续）/ 已不存在（自动跳过）
4. 用户可选择"仍然执行"或"取消"

**影响范围**：前端 + Agent 数据模型
- `OrganizingPlanItem` 增加 `fileSize` 和 `modificationDate` 字段
- `executeOrganizingPlan()` 增加预检查步骤
- 新增确认对话框 UI

**预估代码量**：~120 行
**性能影响**：预检查需遍历文件元数据（~200 个文件约 0.5 秒），在后台队列执行，不阻塞 UI
**体积影响**：可忽略

---

#### P0-4：Quick Look 跟随选中项

**问题描述**：
当前用户选中文件后，需要手动点击详情面板的"快速预览"按钮才能触发 Quick Look。macOS 原生体验（Finder）是选中文件后按空格即可预览。当前交互割裂，降低了连续预览多个文件的效率。

**预期方案**：
- 在待分类列表和整理计划列表上，**监听键盘空格键**事件，触发当前选中文件的 Quick Look
- 在详情面板中选中文件时，如果 Quick Look 面板已打开，**自动切换预览内容**到当前选中文件
- 支持方向键在列表中导航时，Quick Look 实时跟随（可选，通过设置开关控制）

实现方式：
- 使用 `.onKeyPress(.space)` 修饰符监听空格键
- `QuickLookCoordinator` 增加 `updatePreview(url:)` 方法，在面板已打开时更新预览项

**影响范围**：仅前端（`PendingInboxView`、`OrganizingPlanView`、`QuickLookCoordinator`）
**预估代码量**：~50 行
**性能影响**：无
**体积影响**：可忽略

---

### 4.2 P1 — 应该做（v2.5.0 或 v2.6.0）

这些需求能显著提升体验，但优先级低于 P0。

---

#### P1-1：整理历史搜索

**问题描述**：
`HistoryView` 的 500 条记录是完整列表，无搜索功能。用户想找回几天前误移动的某个文件时，只能逐条翻阅。

**预期方案**：
在整理历史页面顶部增加搜索框：
- 搜索文件名、原路径、目标路径、原因（规则名）
- 前端实时过滤（本地数据，不触发磁盘 I/O）
- 搜索结果高亮匹配关键词

**影响范围**：仅前端（`HistoryView`）
**预估代码量**：~60 行
**性能影响**：无（纯内存过滤）

---

#### P1-2：待分类文件总览统计

**问题描述**：
用户打开"待分类"页面时，只知道有 N 个文件，不知道总大小、扩展名分布、文件时间跨度等。这些信息有助于用户判断是否需要新增规则。

**预期方案**：
在待分类列表顶部（搜索栏下方或折叠面板中）显示轻量统计：

```
📊 127 个文件 · 总大小 1.8 GB
扩展名分布：PDF(34) JPG(28) ZIP(18) DMG(12) DOCX(11) ...
最早文件：2026-03-15 · 最新文件：2026-07-16
```

统计在 `refreshPendingFiles()` 时一并计算，缓存到 `@Published` 属性中。

**影响范围**：仅前端（`PendingInboxView` + `AppModel` 增加统计属性）
**预估代码量**：~70 行
**性能影响**：统计计算在文件列表刷新时一次性完成（200 文件约 0.3 秒）

---

#### P1-3：整理计划按目标目录分组显示

**问题描述**：
`OrganizingPlanView` 是平铺列表，当文件分布在 8 个目标目录时，用户难以按目录逐个审查。

**预期方案**：
列表按目标目录分组，使用 `DisclosureGroup` 折叠：
- 每组标题显示：目录路径 + 文件数量 + 总大小
- 默认全部展开
- 支持按组全选/全不选
- 保持现有的单文件选择/取消选择能力

**影响范围**：仅前端（`OrganizingPlanView`）
**预估代码量**：~100 行
**性能影响**：无

---

#### P1-4：规则测试支持选择真实文件

**问题描述**：
当前规则测试（"文件名测试"）只能手动输入文件名，用户无法快速验证真实文件是否能命中规则。

**预期方案**：
在规则测试输入框旁增加"选择文件…"按钮：
- 弹出 `NSOpenPanel`，限制为选择单个文件
- 选择后自动填入文件名并触发测试
- 可选：支持从待分类列表中拖拽文件名到测试输入框

**影响范围**：仅前端（`RulesView` 规则测试区域）
**预估代码量**：~30 行
**性能影响**：无

---

#### P1-5：忽略列表管理界面

**问题描述**：
当前忽略文件通过签名持久化到 `UserDefaults`（`ignoredDefaultsKey`），但用户无法查看、搜索或清空忽略列表。如果误忽略了重要文件，没有恢复途径。

**预期方案**：
在设置页面增加"忽略列表管理"区域：
- 显示忽略文件数量
- "查看忽略列表"按钮，弹出 Sheet 显示所有被忽略文件（路径 + 忽略时间）
- 支持单项取消忽略、全部清空
- 需要在 `UserDefaults` 中记录忽略时间戳（当前仅存储签名）

数据模型：
```
ignoredUnmatchedSignatures: [String: (FileSignature, Date)]
```

**影响范围**：前端（新增 UI）+ Agent 数据格式微调
**预估代码量**：~80 行
**性能影响**：无

---

### 4.3 P2 — 可以做（v2.7.0+，低优先级）

这些需求有价值但非紧迫，或在当前架构下实现成本较高。

---

#### P2-1：重复文件检测提示

**问题描述**：
同名同类文件（如多次下载 `report.pdf`）进入 Downloads 时无提示。虽然 Agent 不会覆盖（自动编号），但用户可能不知情。

**预期方案**：
在待分类列表中，检测同名文件并标记：
- 同名文件以 ⚠️ 图标或橙色标签标记
- 悬浮提示显示："Downloads 中有 3 个同名文件：report.pdf、report_1.pdf、report_2.pdf"
- 此为纯前端检测，不触发额外磁盘 I/O（仅比较 `pendingFiles` 中的文件名）

**影响范围**：仅前端
**预估代码量**：~40 行

---

#### P2-2：撤销后抑制自动整理

**问题描述**：
v2.4.4 已通过独立锁修复撤销与自动整理的锁冲突，但用户体验不完美：撤销文件回到 Downloads → LaunchAgent 检测目录变化 → 自动整理再次触发。虽然撤销时已标记文件为"已见"（`reason: "undo"`），但极端情况下仍有抖动。

**预期方案**：
在 `undo()` 完成后，Agent 向 `state.json` 写入一个短期冷却标记：
```json
{ "undo_cooldown_until": 1800000000 }
```
Agent 在 `runEvent()` 循环开始时检查冷却标记，冷却期（默认 5 秒）内跳过所有新文件处理。

**影响范围**：Agent（`AIFileSorterAgent.swift`）
**预估代码量**：~30 行

---

#### P2-3：菜单栏状态小组件

**问题描述**：
当前菜单栏只有一个图标 + 文字状态。用户无法从菜单栏快速看到"待分类文件数量"。

**预期方案**：
在菜单栏图标上增加角标数字（待分类文件数量），类似 Mail.app 的未读计数。或在菜单栏下拉菜单中显示摘要。

**影响范围**：仅前端（`MenuBarExtra` 部分）
**预估代码量**：~40 行

---

#### P2-4：扩展名过滤的自定义

**问题描述**：
当前 `supported_extensions` 硬编码了约 30 种文件类型，用户无法添加自定义扩展名（如 `.svg`、`.json`、`.log`）。

**预期方案**：
在设置页面增加"支持的文件扩展名"编辑区：显示当前扩展名标签列表 + "添加扩展名…"按钮 + 每个标签可删除。

**影响范围**：前端（设置页）+ Agent 配置验证
**预估代码量**：~70 行

---

## 5. 不作为清单（本阶段明确不做）

| 需求 | 原因 |
|------|------|
| AI 内嵌 / 本地模型推理 | 违背"不上传、不内置 AI"的核心定位；需引入模型文件（>50MB），App 体积膨胀 10 倍以上 |
| iCloud / NAS / 云盘集成 | 违背"纯本地"约束；云盘 API 权限复杂，且 Scanner 无法跨卷操作 |
| Web 管理面板 / Docker 部署 | 违背"轻量"约束；引入 Web 服务增加攻击面 |
| 永久删除文件功能 | 用户明确偏好"不删文件"；已有的撤销机制即是安全网 |
| 图片/视频内容识别 | 需引入 CoreML 模型或 Vision 框架，增加 App 体积和复杂度，且准确率不可靠 |
| 规则推荐引擎 | 需要用户行为数据分析，违背隐私原则 |
| 多语言国际化 | 当前用户群为中文用户，非紧迫需求 |
| 代码重构为大模块拆分 | v2.5 阶段重点在功能体验，架构拆分留到 v3.0 |
| 单元测试覆盖率提升 | 当前 7 个 Python 测试 + Agent 端到端测试已覆盖核心路径 |

---

## 6. 预估影响评估

| 维度 | v2.5.0 (P0) | v2.6.0 (P0+P1) | 说明 |
|------|------------|----------------|------|
| **App 体积增量** | < 0.1 MB | < 0.2 MB | 纯代码增量，无新资源/依赖 |
| **内存增量（空闲）** | < 2 MB | < 5 MB | 增加的统计缓存和过滤状态 |
| **启动时间增量** | < 0.05 秒 | < 0.1 秒 | 统计数据在首次进入对应页面时才计算 |
| **代码行数增量** | ~350 行 | ~700 行 | 集中在 `AIFileSorterApp.swift` |
| **Agent 代码变更** | ~30 行 | ~50 行 | 仅 P0-3 和 P2-2 涉及 Agent |
| **新增 UI 组件** | 搜索栏、统计栏、确认对话框 | +历史搜索、忽略管理界面 | 全部为 SwiftUI 原生组件 |
| **破坏性变更** | 无 | 无 | 所有新增为增量功能 |
| **macOS 13 兼容性** | 不影响 | 不影响 | 使用 SwiftUI 13+ API |

---

## 7. 建议发布节奏

| 版本 | 内容 | 预计迭代 |
|------|------|---------|
| **v2.5.0** | P0-1 搜索筛选 + P0-2 统计分组 + P0-3 执行前确认 + P0-4 Quick Look 跟随 | 1 个迭代 |
| **v2.5.1** | P0 的 bug 修复 + 微调 | 0.5 个迭代 |
| **v2.6.0** | P1-1 历史搜索 + P1-2 总览统计 + P1-3 分组显示 + P1-4 规则测试选文件 + P1-5 忽略列表管理 | 1 个迭代 |
| **v3.0.0** | 架构升级：`AIFileSorterApp.swift` 拆分为多个模块文件；Agent 独立 Target；引入 Swift Testing 框架 | 2 个迭代 |

---

## 8. 附录：v2.4.4 技术架构速查

```
AIFileSorterApp.swift (2304 行)
├── 数据模型：SorterRule, SorterConfig, PendingFile, OrganizingPlanItem, MoveHistory
├── AppModel (ObservableObject)：配置管理、Agent 调用、文件操作、状态刷新
├── 视图：SidebarContentView → PendingInboxView / RulesView / HistoryView / OverviewView
├── Sheet：OrganizingPlanView, PendingMoveSheet, LogsView, WelcomeView
├── 辅助：QuickLookCoordinator, ApplicationDelegate, EmptyStateView, StatusCard
└── MenuBarExtra：状态显示、快捷操作

AIFileSorterAgent.swift (640 行)
├── 数据模型：AgentConfig, AgentRule, AgentState, MoveHistoryRecord, FileSignature
├── NativeSorter：规则匹配、文件移动（native/Finder）、历史记录、撤销
├── 锁机制：sorter.lock (自动), sorter-manual.lock (手动), sorter-undo.lock (撤销)
└── CLI 入口：--once, --run, --move-once, --sort-paths, --undo, --undo-batch, --check-config
```

---

> **文档状态**：待评审。请架构师（Bob）评估技术可行性后进入开发排期。
