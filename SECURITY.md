# 安全政策

AI File Sorter 会处理用户本机文件，因此我们特别关注意外移动、覆盖、路径绕过、权限边界和敏感信息泄露。

## 支持范围

仅最新公开发布版本接受安全修复。请先确认问题能在未修改的最新版本上重现。

## 报告方式

请不要公开 Issue 中披露可利用细节、真实文件名、绝对路径、日志或任何凭据。

优先使用仓库 Security 页面中的 **Report a vulnerability** 私密报告入口（若该入口可见）。如果暂时没有该入口，请先创建一个不含技术细节的 Issue，请求维护者提供私密沟通方式。

报告应包含：受影响版本、最小复现步骤、实际风险、预期结果，以及已知的临时缓解方法。我们会在确认后协调修复和公开说明。

## 不属于安全漏洞的情况

- 用户主动配置了错误或不存在的目标路径。
- macOS 拒绝访问尚未授权的文件夹。
- 未匹配规则的文件保持原位。

这些情况可以通过普通 Issue 讨论，但同样不要附上敏感文件数据。
